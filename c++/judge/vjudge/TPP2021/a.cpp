#include<bits/stdc++.h>
using namespace std;

int main(){
    int a, b, temp;
    int A[10000], B[10000];
    cin >> a >> b;
    for(int i = 0; i < a; i++){
        cin << A[i];
    }
    for(int i = 0; i < b; i++){
        cin << B[i];
    }
    sort(A, A+a);
    sort(B, B+b);

    int i = 0; j = 0;
    int ic = 0; jc = 0;
    
    jp = 0;
    for(int i = 0; i < a; i++){
        while(B[jp] < A[i]){
            if
        }
    }
}
